<Window x:Class="CarRepairShop.Presentation.Wpf.Views.MainWindowView"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Car Repair Shop"
        Height="450" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Width="100" Command="{Binding AddOrderCommand}">Add order</Button>
            <Button Grid.Row="2" Width="100">Assign mechanic</Button>
            <Button Grid.Row="4" Width="100">Complete order</Button>
            <Button Grid.Row="6" Width="100">Show report</Button>
        </Grid>
        <ContentPresenter Grid.Column="2" Margin="5" Content="{Binding Current}"/>
    </Grid>
</Window>
